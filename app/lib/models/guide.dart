class Guide {
  // Creates a constant Guia object with a required title and content
  const Guide({
    required this.titulo,
    required this.contenido,
  });

  // Instance variables to store each category of administrative expenses
  final String titulo;
  final String contenido;
}
